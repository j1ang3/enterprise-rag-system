# Historical Direct Prompt-Injection Implementation Note

This public provenance note replaces an internal implementation report referenced by the frozen indirect-injection manifest. Its role was to record that the direct-injection dataset, deterministic rubric, runner, and result serialization had been implemented before the indirect experiment was frozen.

The executable code, tests, dataset, manifest, and machine-readable result artifacts are the public engineering evidence.
