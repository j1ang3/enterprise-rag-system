# Historical Direct Prompt-Injection Evaluation Note

This public provenance note replaces the generated human-readable report used as a frozen input to the later indirect and paired security evaluations. The underlying machine-readable direct-injection cases and result artifacts remain in `evals/security/` and `evals/results/security/`.

The note does not add or change any attack result, metric, case label, model identity, or experiment conclusion.
