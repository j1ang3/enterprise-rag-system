# Frozen Evaluation Index

This directory contains only the fixed chunk and vector artifacts referenced by the public evaluation manifests. Runtime uploads and application indexes are intentionally excluded from source control. The historical `week07_tuning_*` filenames are stable machine/provenance identifiers retained to preserve manifest compatibility.
