# Frozen Security Index Fixtures

This directory contains only the fixed malicious-corpus index artifacts referenced by the public security-evaluation manifest. Runtime security storage and generated acceptance data are intentionally excluded.
